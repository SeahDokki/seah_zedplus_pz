# Local dev scripts (deploy.ps1 and friends) - machine-specific paths
*.ps1

# OS / editor noise
Thumbs.db
desktop.ini
.DS_Store
.vscode/
.idea/

# Asset sources (originals, .blend, downloads). Only what the mod loads is
# versioned, under SZedPlus/42/media/. See ASSETS.md.
Assets/
